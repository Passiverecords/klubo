name: mcp_realm
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.2.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  state_notifier: ^1.0.0
  realm: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
